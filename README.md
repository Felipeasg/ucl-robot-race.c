RobotsProgramming
=================

We use Makefile to compile.

`make task12`
`make task13`
`make task14`

are available.

Tests
=====

Tests are available only for encoders: go in `tests`, and run `make && ./test`